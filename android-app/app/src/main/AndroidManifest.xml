<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml
  Study Shield Agent — requires these special permissions:
    - PACKAGE_USAGE_STATS  : detect which app is in foreground
    - BIND_ACCESSIBILITY_SERVICE : block apps via accessibility
    - SYSTEM_ALERT_WINDOW : show overlay when blocked app opened
    - DEVICE_ADMIN        : prevent uninstall
    - RECEIVE_BOOT_COMPLETED : restart service on reboot
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Core permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- App usage tracking -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Overlay / block screen -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Device admin (prevents uninstall) -->
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />

    <!-- Wake lock to keep monitoring -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- VPN detection -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudyShield">

        <!-- ── Main Activity ── -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Setup / Permission wizard ── -->
        <activity
            android:name=".ui.SetupActivity"
            android:exported="false" />

        <!-- ── Block overlay screen ── -->
        <activity
            android:name=".ui.BlockedActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@style/Theme.StudyShield.Fullscreen"
            android:excludeFromRecents="true" />

        <!-- ── Core foreground monitoring service ── -->
        <service
            android:name=".services.StudyShieldService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
        </service>

        <!-- ── App monitor service (usage stats polling) ── -->
        <service
            android:name=".services.AppMonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- ── Firebase Cloud Messaging ── -->
        <service
            android:name=".services.StudyShieldMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- ── Boot receiver (restart on reboot) ── -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- ── Device Admin receiver (prevents uninstall) ── -->
        <receiver
            android:name=".receivers.DeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
